# 08. 유전 재조합 설계실

- 프로젝트: `genetic-recombination-lab`
- 작성일: 2026-09-15 / 상태: 구현 전 설계 / 우선순위: 8
- 대상: 중학교 심화·고등학교 생명과학·확률 / 활동: 25분
- [공통 원칙](./00-shared-design-principles.md) · [목록](./README.md)

## 1. 질문·차별성

‘같은 부모에게서 왜 서로 다른 조합이 나올까?’ 두 유전자 연관과 재조합률을 배우자·자손 분포로 연결한다.
기존 진화 앱은 세대별 집단 변화를 다루지만 이 앱은 한 세대의 염색체 전달과 조합에 집중한다.
8순위는 시각적 이해 효과가 크지만 감수분열 애니메이션과 확률 모델의 일치를 검수해야 하기 때문이다.

## 2. 목표·오개념

- 상동염색체·자매염색분체·대립유전자의 관계를 구분한다.
- 연관된 유전자에서 부모형과 재조합형의 빈도가 달라짐을 설명한다.
- 표본 재조합 비율과 모델의 참 재조합률을 구분한다.
- ‘우성은 흔하거나 유리한 것’, ‘재조합률은 항상 실제 거리와 선형’, ‘한 배우자 결과로 전체 확률을 알 수 있다’를 방지한다.
- 사람의 외모·능력·질병은 대상으로 삼지 않고 가상 생물을 사용한다.

## 3. 학습 흐름

1. 3분: AB/ab 부모에서 어떤 배우자가 나올지 예측한다.
2. 5분: 교차가 없는 감수분열과 한 번 있는 예시를 단계별로 비교한다.
3. 5분: r=0.1 모형에서 100개와 1000개 배우자를 생성한다.
4. 7분: 검정교배 자손 표에서 재조합률을 추정한다.
5. 5분: 두 표본 결과가 다른 이유와 거리 추정의 한계를 기록한다.
- 전이: 연결상이 Ab/aB일 때 어떤 조합이 부모형인지 다시 판단한다.

## 4. 화면·상태

| 화면 | 내용 | 행동 |
|---|---|---|
| 염색체 | 유전자 위치·연결상 | 위치/상 선택 |
| 분열 | 염색분체 단계 | 다음 단계·되돌아보기 |
| 반복 | 표본 크기·분포 | 생성·정지 |
| 추정 | 자손 종류별 개수 | r 계산·비교 |
| 기록 | 예측과 관찰 차이 | 설명 저장 |

setup → meiosisDemo → sampling → inference → review. 연결상 변경 시 기존 표본은 별도 실행으로 보존한다.
대량 생성은 seed와 시행 수를 기록한다. 애니메이션 속도는 확률과 무관하다.

## 5. 모델과 단위

- P0는 두 유전자 A/a, B/b, 이배체 부모의 연결상 AB/ab 또는 Ab/aB.
- 재조합률 r∈[0,0.5]. 연결상이 AB/ab면 P(AB)=P(ab)=(1-r)/2, P(Ab)=P(aB)=r/2.
- 반대 연결상에서는 부모형과 재조합형의 역할을 바꾼다.
- 샘플 수 N∈{20,100,1000,10000}. 표본은 범주 분포에서 seed 기반 추출한다.
- 검정교배 상대는 ab/ab. 자손 표현형이 배우자 조합을 구별할 수 있는 완전 우성·독립 표현 조건을 명시한다.
- r_hat=재조합 자손 수/N. N=0에서는 추정하지 않는다.
- r_hat이 표본 변동으로 0.5를 넘으면 원자료를 유지하며 참 모수의 범위와 표본 추정값을 구분한다.
- 거리 표시는 P0에서 비활성. P1에서 작은 r의 근사 거리와 포화 문제를 별도로 설명한다.
- 염색체 위치 슬라이더는 P0에서 ‘개념적 상대 위치’이며 위치를 r로 자동 선형 변환하지 않는다. r 선택은 별도 조작이다.
- 감수분열 그림의 단일 교차 사례는 네 염색분체 중 두 개가 재조합되는 예시다. 일반 r를 단일 교차 빈도와 동일시하지 않는다.
- 무교차·단일 교차 시연과 대량 범주 샘플러를 별도 모드로 표시한다.

## 6. 데이터·평가

- Parent: phase, loci, alleles. Demo: crossoverSegments, chromatidIds, products.
- Sample: seed, trueR, N, countsByHaplotype, observedR.
- Mission: phaseVisible, counts, targetExplanation, revealPolicy.
- 모든 표현형 이미지 ID는 haplotype 조합과 일대일 검수 대응을 갖는다.
- 자동 판정은 부모형 식별·분자 분모 선택·비율 계산에 사용한다. 확률 표본이 기대값과 정확히 같아야 성공으로 처리하지 않는다.

## 7. Three.js와 대체 화면

3D 염색체는 상동체·염색분체를 색+패턴+라벨로 구분하고 교차 구간을 강조한다.
과장된 크기·두께는 교육용 모형임을 표시한다. 단계별 SVG에서도 같은 ID와 대립유전자 정보를 보여준다.
드래그 없이 다음 단계와 숫자 입력만으로 전체 탐구를 수행할 수 있어야 한다.

## 8. 생성 이미지

- P0 가상 생물의 네 표현형 각 3개체=12장, 확장 36장.
- 예시: ‘같은 형태의 가상 정원 생물, 몸색과 무늬 두 특성만 변경, 동일 구도·크기·밝은 조명, 문자 없음’.
- 유전 대응은 엔진 메타데이터로 정의하고 이미지의 개수나 모양을 자동 정답으로 읽지 않는다.
- 검수: 두 특성 외 체격·배경이 유형별로 달라지는 단서, 현실 유전 법칙을 암시하는 설명.
- 염색체 구조·교차 애니메이션은 생성 이미지가 아닌 벡터/3D 엔진으로 제작한다.

## 9. 범위·단계

- P0: 두 유전자, 두 연결상, 단일 교차 예시, r 기반 표본, 검정교배 추정.
- P1: 세 유전자 지도, 이중 교차, 표본 불확실성 구간. 별도 과학 검수 후 추가한다.
- 제외: 사람 유전 예측, 질병 위험 계산, 자연선택·집단 진화.
- M1: 배우자 확률·표본 엔진 → M2: 연결상 예제 → M3: SVG 분열 → M4: 3D·이미지 → M5: 교과 검수.
- 모듈: haplotypes.ts, recombination.ts, sampler.ts, meiosisDemo.ts, ChromosomeScene.tsx, OffspringTable.tsx. 각각 500줄 미만.

## 10. 검증·완료

- r=0이면 부모형만, r=0.5이면 네 조합의 이론 확률이 각각 0.25.
- AB/ab에서 r=0.1이면 AB·ab 각 0.45, Ab·aB 각 0.05.
- 확률합 1, seed 재현, 연결상 변경에 따른 라벨 대응, N=0 처리 검증.
- 통계 검증은 고정 seed 여러 개의 집계와 적절한 허용구간을 사용하고 한 표본의 정확한 비율을 강제하지 않는다.
- 단일 교차 시연의 네 결과와 라벨을 전수 검사한다.
- 키보드 단계 이동·모바일 표·reduced-motion·SVG 대체를 검증한다.
- gi-pulse는 표본 생성/추정 버튼, 업데이트 내역 제공, VoiceOver 제외.
- 완료: 구조 시연과 확률 모드의 차이를 설명하고, 표본 비율과 참 r를 분리해 저장·재현한다.
