{
  "name": "genetic-recombination-lab",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "08 유전 재조합 설계실 — M1 배우자 확률·표본 엔진 (P0)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:web": "vite build",
    "typecheck": "tsc --noEmit",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json",
    "test": "tsc -p tsconfig.json && node --test dist/tests/*.test.js",
    "clean": "rm -rf dist dist-web"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@types/three": "^0.186.0",
    "three": "^0.186.0"
  }
}
